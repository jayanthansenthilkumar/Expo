# =============================================================
# SPARK'26 — Portainer Environment Variables
# =============================================================
# Copy this file and set your values in Portainer:
#   Stacks → spark → Environment Variables
#
# Or place it alongside the compose file as .env
# =============================================================

# ── TrueNAS Dataset Path ────────────────────────────────────
# Create this folder on your TrueNAS pool with subdirs:
#   /db, /uploads, /init
DATA_PATH=/mnt/pool/appdata/spark

# ── Ports ────────────────────────────────────────────────────
APP_PORT=8085
PMA_PORT=8086

# ── Database Credentials (CHANGE THESE!) ─────────────────────
DB_ROOT_PASSWORD=root_password_change_me
DB_NAME=spark
DB_USER=spark_user
DB_PASSWORD=spark_password

# ── Timezone ─────────────────────────────────────────────────
TZ=Asia/Kolkata
